{
  "name": "parcial2",
  "version": "1.0.0",
  "description": "proyecto",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run watch:css\" \"npm run serve\"",
    "serve": "live-server src",
    "watch:sass": "node-css --output-style expanded src/assets/css/styles.css src/assets/css/styles.css --watch"
  },
  "author": "daniel",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2",
    "node-css": "^0.1.0"
  }
}
